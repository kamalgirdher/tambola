{
  "name": "tambola",
  "version": "0.0.1",
  "description": "Tambola Game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd frontend && npm i && npm run build && cd ../landing && npm i && npm run css-build",
    "start": "node app.js",
    "dev-start": "cd frontend && npm i && npm run build && cd ../ && nodemon app.js"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": "Kashish, Chirag",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "http": "0.0.1-security",
    "socket.io": "^2.4.0"
  },
  "devDependencies": {
    "@types/socket.io-client": "^1.4.32",
    "nodemon": "^2.0.3",
    "prettier": "^2.0.5"
  },
  "engines": {
    "node": "12.x"
  }
}
